# KRIVIO AI — Comprehensive Bug Matrix & Defect Inventory

| ID | Area | Feature | Severity | Priority | Reproduction | Expected Behavior | Actual Behavior | Root Cause | Affected Files | Fix | Verification Test | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **BUG-001** | Backend / AI | Model Version Deprecation & 503 Spike Hang | P1 | High | Call AI Mentor or Product Analysis during demand spike | Gracefully route to available flash model | 503 or 404 error thrown to client | `gemini-2.5-flash` deprecated in 2026; no alternate fallback model was configured in `FALLBACK_MODELS` | `src/server/ai/gemini_client.ts`, `src/server/ai/task_router.ts` | Configure `gemini-3.6-flash` as primary and `gemini-3.5-flash-lite` as automatic fallback | `npm run test:ai` | In Progress |
| **BUG-002** | Scripts / Tools | Database Failure Exit Code & Sample Distortion | P2 | Medium | Run `node scratch/check_products.mjs` against invalid db connection | Process terminates with non-zero exit code and distinguishes total vs sample count | Process could exit with 0 or confuse sample count with total inventory | Lack of `process.exit(1)` on error and lack of separate `COUNT(*)` query labeling | `scratch/check_products.mjs`, `scratch/update_db_products.mjs` | Add explicit `process.exit(1)` in catch blocks and separate `totalProducts` from `sampleSize` | `node scratch/check_products.mjs` | In Progress |
| **BUG-003** | Core / Product | Incomplete Image Collection & Nested Object Crash | P1 | High | View product with image stored in `photos`, `gallery`, or object `{ url: '...' }` | Renders direct product photo accurately without fallback | Image ignored or throws `u.includes is not a function` | `getDirectProductPhoto` checked only 4 fields and assumed array items are raw strings | `src/utils/productThumbnail.ts`, `server.ts` | Recursively check all image fields (`photos`, `gallery`, `thumbnail_url`, `media`), normalize object shapes `{ url, secure_url, src }`, and validate with `isValidImageUrl` | `tsx scratch/test_thumbnail_resolver.mjs` | In Progress |
| **BUG-004** | Backend / Products | Unsafe Image URL String Method Calling | P2 | Medium | Create product with array containing object or null item | Sanitizes array without throwing uncaught TypeError | Server fails with `.includes is not a function` | Line 957 of `server.ts` checks only `typeof rawImageUrls[0] === 'string'` but didn't sanitize entire array | `server.ts` | Robust image array normalization filtering valid strings and URL objects safely | `tsx tests/api_integration.test.ts` | In Progress |
| **BUG-005** | UI / Responsive | Mobile Viewport Form & Table Wrapping | P2 | Medium | Open Products table and B2B Quotation modal at 360px viewport | Tables scroll horizontally or stack cleanly; dialogs fit viewport | Potential horizontal overflow or clipped actions | Missing responsive overflow wrapper and fixed-width containers | `src/components/SellExport/B2BQuotationView.tsx`, `src/components/ProductStudio.tsx` | Ensure all tables and dialogs have `max-w-full overflow-x-auto` and dynamic padding | Visual responsive checks at 360px, 768px, 1280px | In Progress |
| **BUG-006** | AI / Vision | Multimodal Product Analysis Pipeline | P1 | High | Upload real artisan craft image | Analyzes image bytes and returns structured characteristics | Returns static response if key or model fails | Legacy prompt structure and missing secondary model fallback | `src/server/ai/prompt_registry.ts`, `src/server/ai/task_router.ts` | Enforce multimodal binary payload transmission and domain validation | Live image test via test suite | In Progress |
| **BUG-007** | Pricing | Pricing Determinism & Arithmetic Safety | P1 | High | Calculate price for brass lamp vs terracotta diya | Produces distinct prices based on specific material & labor inputs | Same price if AI hallucinated identical values | Incomplete decoupling of arithmetic from LLM generation | `src/server/pricing/pricing_engine.ts`, `server.ts` | Enforce deterministic arithmetic formulas in `PricingEngine.calculate` | `npm run test:ai` Suite 1 | Verified Passing |
| **BUG-008** | Security | User Isolation Across B2B Quotations & Products | P0 | Critical | Authenticated User B attempts to access `/api/quotations/:id` of User A | Rejected with HTTP 404/403 | Data leakage across tenant boundary | Missing or bypassable `user_id` query constraints | `server.ts`, `src/server/quotation/quotation_service.ts` | Parameterized `WHERE id = $1 AND user_id = $2` on all queries | `tsx tests/user_isolation.test.ts` | Verified Passing |
